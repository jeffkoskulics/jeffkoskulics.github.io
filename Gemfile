source "https://rubygems.org"

# Optional: only needed to preview the site locally with `bundle exec jekyll serve`.
# GitHub Pages builds the published site itself and does not use this file.
gem "github-pages", group: :jekyll_plugins
gem "webrick"
